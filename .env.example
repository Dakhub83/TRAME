DATABASE_URL="postgresql://user:password@localhost:5432/trame?schema=public"

# Placeholder admin gate for /admin/* (src/lib/admin-auth.ts) — a single
# shared passcode, not real per-user auth. Replace once real auth exists.
ADMIN_ACCESS_CODE="change-me"
